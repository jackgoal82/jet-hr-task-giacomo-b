import { defineConfig } from 'vite'

// Base path allineato al nome del repository, richiesto da GitHub Pages
// quando il sito è pubblicato su https://<utente>.github.io/<repo>/
export default defineConfig({
  base: '/jet-hr-task-giacomo-b/',
})
